Signature assets for Dienstvertrag (DV)

Add the employer signature + stamp image here to be embedded in the HTML-based Dienstvertrag and included in the generated PDF.

Recommended file name
- signature-stamp.png

Guidelines
- Transparent PNG, around 800–1200 px width for crisp PDF output (2x the display size)
- Landscape orientation; include stamp and signature combined if desired
- Keep background fully transparent

How it will be used
- The DV template will reference the image at "/contracts/signature-stamp.png" (same-origin, no CORS issues)
- It renders in the HTML preview (admin and promotor) and is rasterized into the PDF on download

You can also provide multiple variants if needed; just let us know the exact file name to use in the template.


